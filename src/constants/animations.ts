/**
 * Animation timing constants used across the application
 */
export const ANIMATION_TIMINGS = {
  /** Auto-rotation interval for tab carousel (milliseconds) */
  TAB_AUTO_ROTATE: 10000,
  /** Debounce delay for scroll events (milliseconds) */
  SCROLL_DEBOUNCE: 100,
  /** Canvas animation frame rate interval (milliseconds) */
  CANVAS_FRAME_RATE: 100,
} as const;
